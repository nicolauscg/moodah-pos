########################
# Meta
########################
query UserInfo {
  sessionInfo {
    uid
    name
    username
    db
    companyId {
      id
      validDate
      userPlan {
        value
        label
        selected
      }
    }
  }
}
