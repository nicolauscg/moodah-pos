import { FieldConfig } from "../utility/types/fields";

const posCategoryFields: FieldConfig = {
  posCategory: ["id", "image", "name", "display_name", "parent_id", "sequence"]
};

export default posCategoryFields;
